defaults:
  meraki:
    domains:
      organizations:
        managed: true
        networks:
          devices_claim_rate_limiting:
            enabled: false
            count: 10
            interval: 300
          wireless_ssids:
            eap_override:
              max_retries: 5
              timeout: 5
              eapol_key:
                retries: 3
                timeout_in_ms: 10000
              identity:
                retries: 3
                timeout: 10
